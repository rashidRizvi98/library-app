export const port = process.env.PORT || 4000;

export const fe_url = 'http://localhost:3000';

export const dbConfig = {
    host : "localhost",
    port: "27017",
    userName: "mongoadmin",
    password: "secret",
    dbName: "library-app"
} 